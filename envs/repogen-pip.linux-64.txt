# PyPI packages pinned to the versions used for the validated RepoGen runs.
# Install AFTER creating the conda environment from envs/repogen.linux-64.lock:
#
#   conda create --name repogen --file envs/repogen.linux-64.lock
#   conda activate repogen
#   pip install --no-deps -r envs/repogen-pip.linux-64.txt
#   pip install -e .
#
# --no-deps is deliberate: every transitive dependency is already pinned by the
# conda lock, and letting pip resolve them again can silently upgrade packages.
adjustText==1.3.0
cmapPy==4.0.1
upsetplot==0.9.0
